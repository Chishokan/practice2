# CLAUDE.md

このリポジトリで作業する際の規約。作業開始前に必ず `docs/GAME_DESIGN.md` を読むこと。

## プロジェクト概要

ブラウザ向けノベル×管理シミュレーション『貸出は三日まで』。
表向きは和やかな図書館運営ゲーム。裏では、書架から降ろした本の内容が世界から消える。

**PC・スマホ両対応。個人開発。開発は平日に少しずつ進む。**

## 技術スタック

- Vite + React 18 + TypeScript（strict）
- Zustand（状態管理）
- Tailwind CSS
- Framer Motion（演出）
- localStorage（セーブ）
- Cloudflare Pages（配布）

Next.js は採用しない。SSR・ファイルルーティングは本作に不要。

## アーキテクチャ規約

### 1. `src/domain/` はReactに依存させない

ゲームルールはすべて純粋関数として `domain/` に置く。
`import React` を `domain/` 配下に書いてはならない。

理由：分岐とフラグの整合性をテストで機械的に検証するため。本作は分岐が多く、
これがないと伏線の破綻に気づけない。

```
❌ domain/shelf.ts で useState を使う
✅ domain/shelf.ts は (state, input) => newState の純粋関数のみ
```

### 2. シナリオは TypeScript で書く

`src/content/` 配下。JSON/YAMLは使わない。
フラグIDや BookId の参照ミスを型で検出する。

### 3. 状態変更は必ず domain 経由

UIコンポーネントから `WorldState` を直接書き換えない。
Zustandのアクションは `domain/` の関数を呼ぶだけの薄い層に留める。

### 4. 演出をシーンにハードコードしない

綻び演出（台帳の書き換え、描写の簡素化など）は `domain/anomaly.ts` の
宣言的ルールとして定義する。個別シーンに直接書くと周回時に破綻する。

## 作業の進め方

### 1タスクの粒度

**1コミットで完結し、30分程度で終わる単位に分割すること。**
大きな機能を一度に実装しない。分割案を先に提示してから着手する。

### 実装前に確認する

仕様に曖昧さがある場合、推測で実装せず質問すること。
特に演出の見せ方については、意図と異なる実装をされると手戻りが大きい。

### 動作確認

各フェーズ完了時、`npm run dev` で実際に触れる状態にする。
「型は通るが画面で確認できない」状態でフェーズを終えない。

## 演出上の絶対規則

これらは仕様ではなく作品の核なので、逸脱しないこと。

1. **真相を地の文で説明しない。** 「ズレ」として置くだけにする
2. **システムメッセージで異常を通知しない。**
   「本が消えました」のような表示は絶対に出さない
3. **プレイヤーが「自分の記憶違いかも」と疑える余地を残す**
4. **第1章の和やかさを削らない。** 不気味さは和やかさの中の綻びから生まれる
5. **`--erasure`（消失の紫）を第2章より前に画面に出さない**

## コード規約

- TypeScript strict モード。`any` 禁止
- コンポーネントは関数コンポーネントのみ
- 1ファイル 200行を超えたら分割を検討
- コメントは「なぜ」を書く。「何を」はコードで表現する
- 日本語のテキストデータと、コード上の識別子を混在させない
  （識別子は英語、表示テキストは `content/` の日本語）

## やらないこと（スコープ防衛）

以下は初回リリースでは**実装しない**。提案もしない。

- 立ち絵の表情差分
- 音声・BGM・効果音
- 実績・トロフィー
- オンライン機能・ランキング
- 多言語対応

これらを足したくなったら、まず全3章を完成させること。

例外：案内役（図書館の妖精）の**全身フォーム変化（2状態：基本形／救済時）**は「表情差分」では
なく物語進行の状態であり本作の核。道中は基本形に固定し、救済時のみ切り替える。スコープ内と
する（詳細は `docs/GAME_DESIGN.md` 12.2）。

## テスト

`domain/` に対してのみ Vitest を書く。UIのテストは書かない。

必須のテスト：
- 全エンディングに到達可能であること
- すべての来訪者が、いずれかの周回で登場可能であること
- 消去の波及処理が、参照先の存在しないフラグを生まないこと

## ディレクトリ

```
src/
├── domain/     # ルール（純粋関数のみ、React非依存）
├── store/      # Zustand（domainを呼ぶ薄い層）
├── content/    # シナリオデータ（執筆作業はここだけ）
├── ui/         # 画面・コンポーネント
└── save/       # 永続化
docs/
└── GAME_DESIGN.md
```
